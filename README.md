# Installation

---

1. Build containers
   ```bash
   docker-compose build
   ```

2. Run containers
   ```bash
   docker-compose up -d
   ```

---

Open ports: 5432 (Postgres), 80 (Frontend), 6060 (ML), 8000 (API)