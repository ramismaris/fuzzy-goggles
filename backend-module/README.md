# Gazprom Tech Backend

## Установка и запуск

1. Редактирование .env
   ```bash
   cp .env.dist .env && nano .env
   ```
2. Сборка контейнера
   ```bash
   docker-compose build
   ```
   
3. Запуск контейнера
   ```bash
   docker-compose up -d
   ```
